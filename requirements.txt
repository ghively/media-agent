# Runtime dependencies — pinned for reproducible container builds.
# Bump deliberately and re-run `python -m pytest tests/ -q` after changes.
# yt-dlp in particular needs regular bumps to keep up with YouTube.
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0
langchain-core==1.4.9
langchain-ollama==1.1.0
langchain-openai==1.3.5
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
httpx==0.28.1
rich==15.0.0
PyYAML==6.0.1
apscheduler==3.11.3
aiosqlite==0.22.1
jinja2==3.1.6

# Provider CLIs (invoked as subprocesses by src/providers/*)
yt-dlp==2026.7.4
bandcamp-downloader==0.0.17
internetarchive==5.10.1
mutagen==1.48.1
audible-cli==0.3.3
streamlink==8.4.0

# Telegram interface (roadmap — preinstalled so the bot can ship without an image change)
python-telegram-bot==22.8
